<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmitAppFit"
        tools:targetApi="31">

        <!-- הוספת intent-filter ל-myClosetActivity -->
        <activity
            android:name=".screens.myClosetActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".screens.addItem"
            android:exported="false" />
        <activity
            android:name=".screens.savedLooks"
            android:exported="false" />
        <activity
            android:name=".screens.MainActivity"
            android:exported="false" /> <!-- MainActivity כבר לא תהיה הראשונה -->
        <activity
            android:name=".screens.Register"
            android:exported="true" />
        <activity
            android:name=".screens.LogIn"
            android:exported="false" />
    </application>

</manifest>
